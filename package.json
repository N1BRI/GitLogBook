{
  "name": "gitlogbook",
  "version": "0.1.0",
  "description": "Local ADIF ham radio logger with a read-only GitHub Pages viewer.",
  "type": "commonjs",
  "scripts": {
    "dev": "node server.js",
    "export": "node scripts/export-public.js && node scripts/build-posts.js",
    "posts": "node scripts/build-posts.js"
  },
  "engines": {
    "node": ">=20"
  }
}
